services:
  account-service:
    build: .
    ports:
      - "5000:5000"
    env_file:
      - ./.env
    depends_on:
      - db

  db:
    image: postgres:16-alpine
    env_file:
      - ./.env.db
    ports:
      - "5432:5432"
    volumes:
      - db_data:/var/lib/mysql

volumes:
  db_data:
